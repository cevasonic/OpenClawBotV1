# 2026-06-08

## Nhật ký hoạt động

- **Tạo Skill Quản lý Quỹ & Chi tiêu (`thuchi`):**
  - Đã viết kịch bản Python `manage_fund.py` tại `skills/thuchi/scripts/` để xử lý phân tích cú pháp ngôn ngữ tự nhiên tiếng Việt cho các giao dịch quỹ (thu, chi, số dư, báo cáo).
  - Khắc phục thành công một lỗi phân tích cú pháp ngày làm mất chữ số của số tiền khi người dùng gõ kiểu `ngày 24/6 chi ăn tối 180k`.
  - Đã cập nhật `skills/thuchi/SKILL.md` để hướng dẫn mô hình AI chạy trực tiếp script Python, giảm thiểu tối đa token và triệt tiêu sai sót tính toán.
  - Thiết lập cron job Linux để gửi báo cáo tuần vào lúc 17h00 thứ 6 hàng tuần tới cả Zalo và Telegram qua CLI của OpenClaw.
  - Di chuyển tệp tin cơ sở dữ liệu `fund_management.json` từ `/opt/openclaw/data/` vào thư mục `skills/thuchi/data/` để quản lý skill tập trung và dễ dàng hơn.
  - Sửa lỗi regex làm sạch ngày tháng khiến phần năm (ví dụ `/2026`) bị sót lại và bị nhận diện nhầm thành số tiền `2.026đ`.
  - Cập nhật hàm báo cáo `generate_report` hỗ trợ động tiêu đề và lọc ngày tùy biến khi người dùng yêu cầu báo cáo từ đầu tháng đến hiện tại.
  - Bổ sung khả năng tự động phân tích cú pháp tin nhắn nhiều dòng (multi-line) có phân nhóm theo ngữ cảnh ngày tháng (date context) để ghi nhận dữ liệu ban đầu cực kỳ chính xác.
  - Dọn dẹp cơ sở dữ liệu và nạp lại toàn bộ tệp dữ liệu ban đầu của anh Bình chính xác 100%.
- **Reset và đổ lại dữ liệu Quỹ Chi tiêu (`thuchi`):**
  - Reset cơ sở dữ liệu `fund_management.json`.
  - Phát hiện và sửa lỗi phân loại category trong `manage_fund.py` (đưa điều kiện kiểm tra 'Cà phê' lên trước 'Ăn sáng' để 'Cafe sáng' không bị nhận nhầm thành 'Ăn sáng').
  - Đổ thành công dữ liệu thu chi từ ngày 01/06/2026 đến ngày 08/06/2026 do Anh Bình yêu cầu.
  - Sửa lỗi nhận diện ý định báo cáo chi tiêu: bổ sung các từ khóa như `chi tiêu`, `tổng chi`, `tổng chi tiêu`, `thống kê`, `lịch sử chi` vào `report_keywords` để khi Anh Bình hỏi "tổng chi tiêu..." sẽ hiển thị đầy đủ báo cáo chi tiết thay vì báo lỗi hoặc chỉ ra tổng số tiền.


